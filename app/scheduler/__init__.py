# from apscheduler.schedulers.background import BackgroundScheduler
# from .tasks import create_daily_menus

# scheduler = BackgroundScheduler()

# def init_scheduler():
#     scheduler.add_job(create_daily_menus, trigger='interval', seconds=10)
#     scheduler.start()
